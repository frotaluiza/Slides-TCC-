\section{Metodologia Proposta}

\begin{slide}{Visão Geral do Pipeline}
  \centering
  \vspace*{-0.3cm}
  \resizebox{0.98\textwidth}{!}{%
  \begin{tikzpicture}[
      every node/.style={font=\scriptsize},
      stage/.style={rectangle, rounded corners, draw=poliBlue, thick, align=center,
                    minimum width=2.8cm, minimum height=0.9cm, fill=blue!6},
      detail/.style={rectangle, rounded corners, draw=black!40, thick, align=center,
                     minimum width=2.2cm, minimum height=0.6cm, fill=gray!6},
      arrow/.style={->, thick, >=stealth},
    ]
    \node[stage] (dados)     {Dados\\Experimentais};
    \node[stage, right=0.3cm of dados] (eda) {Análise\\Exploratória};
    \node[stage, right=0.3cm of eda] (fisico) {Modelo\\Físico 0D};
    \node[stage, right=0.3cm of fisico] (s0) {Stage 0\\Clássicos};
    \node[stage, right=0.3cm of s0] (s1) {Stage 1\\Baseline Keras};
    \node[stage, right=0.3cm of s1] (s2) {Stage 2\\Híbridos};
    \node[stage, right=0.3cm of s2] (sel) {Seleção\\1-SE};
    \node[stage, right=0.3cm of sel] (final) {Vencedor /\\0D / Redes};

    \draw[arrow] (dados) -- (eda);
    \draw[arrow] (eda) -- (fisico);
    \draw[arrow] (fisico) -- (s0);
    \draw[arrow] (s0) -- (s1);
    \draw[arrow] (s1) -- (s2);
    \draw[arrow] (s2) -- (sel);
    \draw[arrow] (sel) -- (final);

    \node[detail, below=0.25cm of dados] {174 amostras, 3 regimes};
    \node[detail, below=0.25cm of eda]   {Correlação, dispersão};
    \node[detail, below=0.25cm of fisico] {Código C (Lisboa, 2024)};
    \node[detail, below=0.25cm of s0]     {OLS, Ridge, Lasso,\\ElasticNet, DT, RF, GB};
    \node[detail, below=0.25cm of s1]     {MLP multi-saída\\(128,64,32), ReLU};
    \node[detail, below=0.25cm of s2]     {PhyInput, PhyResidual,\\PhyLoss, PhyHybrid};
    \node[detail, below=0.25cm of sel]    {1-SE + min.\\complexidade};
    \node[detail, below=0.25cm of final]  {Comparação vencedor\\\textit{vs} 0D \textit{vs} redes};
  \end{tikzpicture}
  }
  \vfill
  \begin{columns}
    \begin{column}{0.48\linewidth}
      \textbf{Validação Cruzada: GroupKFold (K=3)}
      \begin{Item}[0.15]
        \item Cada fold treina com 2 regimes e valida no 3°
        \item Impede vazamento entre amostras correlacionadas
      \end{Item}
    \end{column}
    \begin{column}{0.48\linewidth}
      \textbf{Seleção: 1-SE + complexidade}
      \begin{Item}[0.15]
        \item Modelos dentro de 1 desvio padrão do melhor
        \item Entre eles, o de menor complexidade
      \end{Item}
    \end{column}
  \end{columns}
  \note{Visão geral do pipeline em 7 etapas: dados, análise exploratória, modelo físico 0D, Stage 0 com modelos clássicos, Stage 1 com baseline Keras, Stage 2 com arquiteturas híbridas, seleção pelo critério 1-SE e comparação final. A validação usa GroupKFold com K=3, onde cada fold segura um regime inteiro fora do treino. A seleção combina 1-SE com mínima complexidade. [~2 min]}
\end{slide}
%========================================================================

\begin{slide}{Validação Cruzada e Seleção de Modelos}
  \begin{columns}
    \begin{column}{.5\linewidth}
      \textbf{GroupKFold por Regime}
      \begin{Item}[0.15]
        \item K=3 partições: um regime reservado p/ validação
        \item Grupo = Regime (10, 40, 70 g/L)
        \item Evita vazamento entre amostras correlacionadas
      \end{Item}
      \vspace{0.1cm}
      \includegraphics[width=\linewidth]{tcc_images/chap04/group_k_fold_custom.png}
      \vspace{0.05cm}
      \scriptsize \textcolor{gray}{Partição dos 3 regimes em cada fold do GroupKFold. Fonte: Elaborado pela autora.}
    \end{column}
    \begin{column}{.5\linewidth}
      \textbf{Critério de seleção (1-SE)}
      \begin{Item}[0.15]
        \item 1\raisebox{-0.5ex}{\textordmasculine}: melhor RMSE$_{CV}$ no fluxo
        \item 2\raisebox{-0.5ex}{\textordmasculine}: modelos dentro de 1 desvio padr\~ao do melhor
        \item 3\raisebox{-0.5ex}{\textordmasculine}: menor complexidade entre eles
      \end{Item}
      \vspace{0.3cm}
      \textbf{Split Treino/Val/Teste}
      \begin{Item}[0.15]
        \item 80\% treino+val: submetido ao GroupKFold (3 folds)
        \item 20\% hold-out: separado \textbf{antes} de qualquer treino
        \item Hold-out contém amostras dos 3 regimes
      \end{Item}
    \end{column}
  \end{columns}
  \note{A validação usa GroupKFold com 3 partições. A cada iteração, um regime fica de fora — então temos 3 valores de RMSE, um para cada fold. O desvio padrão desses 3 RMSEs mede o quanto o desempenho do modelo varia quando testado em regimes diferentes. Se um modelo tem RMSE médio de 0,070 com desvio de 0,010, isso significa que o RMSE pode variar cerca de 0,010 para mais ou para menos dependendo do regime. O critério 1-SE diz o seguinte: calculamos o RMSE médio de cada modelo entre os 3 folds. Pegamos o melhor modelo. Depois, consideramos todos os modelos cujo RMSE médio está dentro de 1 desvio padrão acima do melhor. Entre esses, escolhemos o mais simples. A lógica é: se a diferença entre dois modelos é menor que a própria variabilidade da avaliação, não dá para dizer que um é realmente melhor — então fica com o mais simples. [~2 min]}
\end{slide}
%========================================================================

\begin{slide}{Busca de Hiperparâmetros}
  \footnotesize
  \centering
  \begin{tabular}{p{0.22\linewidth} p{0.70\linewidth}}
    \toprule
    \textbf{Família} & \textbf{O que foi ajustado} \\
    \midrule
    OLS & Sem parâmetros. Referência linear. \\
    Ridge / Lasso / ElasticNet & Intensidade da regularização ($\alpha$) e proporção L1/L2. \\
    Árvore de decisão & Profundidade, folhas mínimas e poda por custo-complexidade. \\
    Random Forest & Número de árvores, profundidade e fração de \textit{features}. \\
    Gradient Boosting & Taxa de aprendizado, profundidade e subamostragem. \\
    MLP (sklearn) & Arquitetura (camadas/neurônios), regularização L2, taxa de aprendizado. \\
    KerasMLP (baseline) & Arquitetura, L2, taxa de aprendizado, otimizador. \\
    Híbridos & Busca restrita: apenas L2, mantendo a baseline fixa. \\
    \bottomrule
  \end{tabular}
  \vfill
  \begin{columns}
    \begin{column}{0.48\linewidth}
      \textbf{Critério de seleção:}
      \begin{Item}[0.15]
        \item Grid search (clássicos) ou busca aleatória (Keras)
        \item 1-SE + mínima complexidade dentro de cada família
      \end{Item}
    \end{column}
    \begin{column}{0.48\linewidth}
      \textbf{Busca por alvo:}
      \begin{Item}[0.15]
        \item Cada família otimizada separadamente para Flux, Alim e Ref
        \item 3 conjuntos de parâmetros por família
      \end{Item}
    \end{column}
  \end{columns}
  \note{A busca foi organizada por família. Nos lineares, o foco é a intensidade da regularização. Nas árvores, controlamos profundidade e número de estimadores. Nas redes neurais, a busca envolve arquitetura, taxa de aprendizado e regularização L2. Já nos híbridos, a busca foi restrita: mantivemos toda a estrutura da baseline fixa e variamos apenas o L2, para avaliar o efeito da incorporação da física sem alterar a arquitetura. [~1,5 min]}
\end{slide}
%========================================================================

\begin{slide}{Arquiteturas Híbridas}
  \small
  \centering
  \resizebox{\textwidth}{!}{
    \begin{tabular}{p{0.2\linewidth} p{0.3\linewidth} p{0.2\linewidth} p{0.2\linewidth}}
      \toprule
      \textbf{Arquitetura} & \textbf{Como integra física + dados} & \textbf{Pontos fortes} & \textbf{Limitações} \\
      \midrule
\textbf{PhyInput}
      & Entrada expandida: $[X, Y_{phy}]$ — física como feature extra
      & Simples; rede explora correlações
      & Pode ignorar física \\
      \midrule
      \textbf{PhyResidual}
      & $Y_{res} = Y - Y_{phy}$; rede aprende correção residual
      & Melhor RMSE para fluxo
      & Performance nas T$_{out}$ inferior ao linear \\
      \midrule
      \textbf{PhyHybrid}
      & Combina PhyInput + Residual: entrada expandida + correção
      & Mais flexível
      & Maior complexidade \\
      \midrule
      \textbf{PhyLoss}
      & Regularização física na função de perda
      & Preserva consistência
      & Menor ganho prático \\
      \bottomrule
    \end{tabular}
  }
  \note{Vamos às arquiteturas híbridas. A primeira é o PhyInput, Hybrid Physics Data. A ideia é simples: pegamos as predições do modelo físico 0D e concatenamos como variáveis de entrada extras, formando um vetor expandido [X, Y_phy]. A rede então aprende a relação entre esse conjunto expandido e as saídas experimentais, podendo explorar complementaridades entre os dados medidos e a física. A terceira é o PhyResidual, que usa modelagem residual. O modelo físico dá uma estimativa inicial, e a rede neural aprende uma correção para somar a ela: Y_hat = Y_phy + Y_res. A rede não precisa aprender o comportamento inteiro do zero — só o desvio em relação ao modelo físico. A quarta é o PhyHybrid, Hybrid Residual Neural Network, que combina as duas anteriores: a rede recebe tanto as variáveis experimentais quanto a predição física como entrada, E produz uma correção residual somada à física. É a arquitetura mais flexível, mas também a mais complexa. Por fim, o PhyLoss não altera a arquitetura da rede — em vez disso, adiciona um termo na função de perda que penaliza a diferença entre a predição da rede e a predição física. A função objetivo passa a ter dois termos: o erro em relação aos dados experimentais e uma penalização pelo desvio em relação ao modelo físico. [~3 min]}
\end{slide}
%========================================================================


